import React from 'react';
import ReactDOM from 'react-dom/client'; // <-- aqui é "react-dom/client"
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
